package es.altia.bne.postulante.domain.model;

public class Example {
    /**
     * Es una entidad creada para poder realizar reglas de negocio Ejemplo en BBDD
     * (entity) tenemos precio e iva, en esta capa se realizan las reglas de negocio
     * para aplicar el iva y devolvemos una entidad con un atributo "precio final"
     * que no tenia la entity. En la capa de applicacion se mapea a DTO para poder
     * devolverlo a la capa de presentación
     */
}
